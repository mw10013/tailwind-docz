---
name: Button
---

import { Playground } from "docz";
import { Button } from "./Button";

# Button

<Playground>
  <div className="flex justify-around">
    <Button variant="primary" size="medium">
      Button text
    </Button>
    <Button variant="secondary" size="medium">
      Button text
    </Button>
    <Button variant="white" size="medium">
      Button text
    </Button>
  </div>
  <div className="flex justify-around mt-4">
    <Button variant="primary" size="medium" disabled>
      Button text
    </Button>
    <Button variant="secondary" size="medium" disabled>
      Button text
    </Button>
    <Button variant="white" size="medium" disabled>
      Button text
    </Button>
  </div>
  <div className="flex justify-around mt-4">
    <Button variant="primary" size="medium" shape="round">
      Button text
    </Button>
    <Button variant="secondary" size="medium" shape="round">
      Button text
    </Button>
    <Button variant="white" size="medium" shape="round">
      Button text
    </Button>
  </div>
</Playground>
